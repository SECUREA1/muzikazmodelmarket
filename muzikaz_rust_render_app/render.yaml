services:
  - type: web
    name: muzikaz-rust-store
    env: rust
    plan: free
    buildCommand: cargo build --release
    startCommand: ./target/release/muzikaz_store
    envVars:
      - key: PORT
        value: 10000
