import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.160.0/+esm';
import { GLTFLoader } from 'https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/loaders/GLTFLoader.js/+esm';

// BEEDEERE is the repository's non-BeeDuck bee asset.  Keeping this cache at
// module scope makes React/dev re-entry and level restarts share one download.
export const BEE_GLB_PATH = 'public/models/BEEDEERE.glb';
export const BEE_CONFIG = Object.freeze({ hoverHeight:2.5, minimumGroundClearance:1.75, maximumHoverCorrection:4, hoverSpeed:2.1, hoverAmount:.18, detectionRadius:13, attackRange:7, retreatDistance:2.75, retreatResumeDistance:4.25, attackCooldown:1.25, projectileSpeed:9, projectileLifetime:3, honeyDamage:7, playerDamageCooldown:.7, sprayCount:3, spraySpread:.1, maxHealth:300, gunDamage:2, laserDamage:2, batDamage:3, roamRadius:8 });
let beeAssetPromise;
function loadBeeAsset() { return beeAssetPromise ||= new GLTFLoader().loadAsync(BEE_GLB_PATH); }
function disposeObject(root) { root?.traverse(o => { if (!o.isMesh) return; o.geometry?.dispose?.(); const materials=Array.isArray(o.material)?o.material:[o.material]; materials.forEach(m=>m?.dispose?.()); }); }

export class AirborneHoneyBee {
  constructor({scene, environmentMeshes, getPlayerPosition, damagePlayer, status, onDefeated, spawn}) { this.scene=scene; this.environmentMeshes=environmentMeshes; this.getPlayerPosition=getPlayerPosition; this.damagePlayer=damagePlayer; this.status=status; this.onDefeated=onDefeated; this.spawn=spawn.clone(); this.root=new THREE.Group(); this.root.name='MUZIKAZ_LEVEL_ONE_AIRBORNE_HONEY_BEE'; this.root.userData.beeController=this; this.state='WANDER'; this.health=BEE_CONFIG.maxHealth; this.projectiles=[]; this.puddles=[]; this.raycaster=new THREE.Raycaster(); this.lastGroundY=null; this.destination=spawn.clone(); this.destinationAge=0; this.lastAttack=-Infinity; this.lastPlayerDamage=-Infinity; this.defeatAge=0; this.isDisposed=false; this.ready=false; this.modelBottom=0; this.mixer=null; this.scene.add(this.root); }
  async load() { try { const gltf=await loadBeeAsset(); if(this.isDisposed)return false; const model=gltf.scene.clone(true); model.name='Honey bee GLB'; model.updateMatrixWorld(true); let box=new THREE.Box3().setFromObject(model),size=box.getSize(new THREE.Vector3()),largest=Math.max(size.x,size.y,size.z)||1; model.scale.setScalar(1.45/largest); model.updateMatrixWorld(true); box.setFromObject(model); const center=box.getCenter(new THREE.Vector3()); model.position.sub(center); model.updateMatrixWorld(true); this.modelBottom=new THREE.Box3().setFromObject(model).min.y; model.traverse(mesh=>{if(mesh.isMesh){mesh.geometry=mesh.geometry.clone();mesh.material=Array.isArray(mesh.material)?mesh.material.map(m=>m.clone()):mesh.material.clone();mesh.castShadow=true;mesh.receiveShadow=true;mesh.userData.beeController=this;}}); this.root.add(model); if(gltf.animations?.length){this.mixer=new THREE.AnimationMixer(model); gltf.animations.forEach(clip=>this.mixer.clipAction(clip).play());} else this.addProceduralWings(); this.ready=true; return true; } catch(error) { console.warn(`[RAD-TOX] Honey bee GLB failed to load (${BEE_GLB_PATH}); using a safe procedural fallback.`,error); this.status?.('Honey bee model failed to load — procedural airborne fallback active.'); this.addFallbackBee(); this.ready=true; return false; } }
  addProceduralWings() { const mat=new THREE.MeshStandardMaterial({color:0xffe66d,transparent:true,opacity:.72,side:THREE.DoubleSide}); this.wings=[-1,1].map(side=>{const wing=new THREE.Mesh(new THREE.CircleGeometry(.42,14),mat);wing.name='Procedural bee wing';wing.position.set(side*.32,.25,0);wing.rotation.y=Math.PI/2;this.root.add(wing);return wing;}); }
  addFallbackBee() { const body=new THREE.Mesh(new THREE.SphereGeometry(.46,16,12),new THREE.MeshStandardMaterial({color:0xf6b817,emissive:0x704000,emissiveIntensity:.35})); const stripe=new THREE.Mesh(new THREE.TorusGeometry(.35,.055,8,16),new THREE.MeshStandardMaterial({color:0x241907})); stripe.rotation.x=Math.PI/2; this.root.add(body,stripe); this.modelBottom=-.46; [body,stripe].forEach(mesh=>mesh.userData.beeController=this); this.addProceduralWings(); }
  groundAt(position) { const meshes=this.environmentMeshes().filter(m=>m.visible!==false); if(!meshes.length)return null; const origin=new THREE.Vector3(position.x,Math.max(position.y+12,20),position.z); this.raycaster.set(origin,new THREE.Vector3(0,-1,0)); const hit=this.raycaster.intersectObjects(meshes,true).find(h=>{const n=h.face?.normal.clone().transformDirection(h.object.matrixWorld); return n?.y>.5 && !/(ceiling|roof|wall)/i.test(h.object.name||'');}); return hit||null; }
  setSpawnFromGround() { const hit=this.groundAt(this.spawn); if(hit)this.lastGroundY=hit.point.y; this.root.position.set(this.spawn.x,(this.lastGroundY??this.spawn.y)+BEE_CONFIG.minimumGroundClearance-this.modelBottom,this.spawn.z); }
  update(delta, elapsed) { if(this.isDisposed||!this.ready)return; if(this.state==='DEFEATED'){this.defeatAge+=delta;this.root.rotation.z+=delta*8;this.root.scale.multiplyScalar(Math.max(.01,1-delta*1.5));if(this.defeatAge>1)this.dispose();return;} this.mixer?.update(delta); if(this.hitFlashUntil&&performance.now()>this.hitFlashUntil){this.root.rotation.z=0;this.hitFlashUntil=0;} else if(this.hitFlashUntil)this.root.rotation.z=.22; this.wings?.forEach((w,i)=>w.rotation.z=Math.sin(elapsed*18+i*Math.PI)*.7); const player=this.getPlayerPosition(),toPlayer=player.clone().sub(this.root.position);toPlayer.y=0;const distance=toPlayer.length(); if(distance<BEE_CONFIG.retreatDistance)this.state='RETREAT'; else if(this.state==='RETREAT'&&distance<BEE_CONFIG.retreatResumeDistance){} else if(distance<BEE_CONFIG.attackRange)this.state='ATTACK'; else if(distance<BEE_CONFIG.detectionRadius)this.state='CHASE'; else this.state='WANDER'; let direction=new THREE.Vector3(); if(this.state==='WANDER'){this.destinationAge-=delta;if(this.destinationAge<=0||this.root.position.distanceTo(this.destination)<.8){const a=Math.random()*Math.PI*2,r=Math.random()*BEE_CONFIG.roamRadius;this.destination.set(this.spawn.x+Math.cos(a)*r,0,this.spawn.z+Math.sin(a)*r);this.destinationAge=2+Math.random()*3;}direction.subVectors(this.destination,this.root.position);direction.y=0;} else if(this.state==='RETREAT')direction.copy(toPlayer).negate().add(new THREE.Vector3(-toPlayer.z,0,toPlayer.x).multiplyScalar(.35)); else direction.copy(toPlayer); if(direction.lengthSq()) {direction.normalize(); const speed=this.state==='ATTACK'?0:this.state==='RETREAT'?3:2.15;this.root.position.addScaledVector(direction,speed*delta);this.root.rotation.y=THREE.MathUtils.damp(this.root.rotation.y,Math.atan2(direction.x,direction.z),7,delta);} if(this.root.position.distanceTo(this.spawn)>BEE_CONFIG.roamRadius*1.25){const home=this.spawn.clone().sub(this.root.position);home.y=0;this.root.position.addScaledVector(home.normalize(),delta*3);} const hit=this.groundAt(this.root.position); if(hit)this.lastGroundY=hit.point.y; if(this.lastGroundY!==null){const bob=Math.sin(elapsed*BEE_CONFIG.hoverSpeed)*BEE_CONFIG.hoverAmount,target=this.lastGroundY+BEE_CONFIG.minimumGroundClearance-this.modelBottom+bob;this.root.position.y=THREE.MathUtils.damp(this.root.position.y,target,5,delta);this.root.position.y=Math.max(this.root.position.y,this.lastGroundY+BEE_CONFIG.minimumGroundClearance-this.modelBottom); } if(this.state==='ATTACK'&&elapsed-this.lastAttack>BEE_CONFIG.attackCooldown){this.lastAttack=elapsed;this.fireHoney(player);} this.updateProjectiles(delta,elapsed); }
  fireHoney(player) { const start=this.root.position.clone().add(new THREE.Vector3(0,.05,0)),base=player.clone().sub(start).normalize(); for(let i=0;i<BEE_CONFIG.sprayCount;i++){const velocity=base.clone();velocity.x+=(i-(BEE_CONFIG.sprayCount-1)/2)*BEE_CONFIG.spraySpread;velocity.normalize().multiplyScalar(BEE_CONFIG.projectileSpeed);const mesh=new THREE.Mesh(new THREE.SphereGeometry(.11,10,8),new THREE.MeshStandardMaterial({color:0xffcf22,emissive:0xb87a00,emissiveIntensity:1}));mesh.position.copy(start);this.scene.add(mesh);this.projectiles.push({mesh,velocity,age:0});} }
  updateProjectiles(delta,elapsed) { for(const p of [...this.projectiles]){p.age+=delta;p.velocity.y-=3*delta;p.mesh.position.addScaledVector(p.velocity,delta);const player=this.getPlayerPosition();if(p.mesh.position.distanceTo(player)<.55&&elapsed-this.lastPlayerDamage>BEE_CONFIG.playerDamageCooldown){this.lastPlayerDamage=elapsed;this.damagePlayer(BEE_CONFIG.honeyDamage,'Honey spray hit!');this.removeProjectile(p);continue;}const hit=this.groundAt(p.mesh.position);if(hit&&p.mesh.position.y<=hit.point.y+.12){this.createPuddle(hit);this.removeProjectile(p);continue;}if(p.age>BEE_CONFIG.projectileLifetime)this.removeProjectile(p);} for(const puddle of [...this.puddles]){puddle.age+=delta;if(puddle.mesh.position.distanceTo(this.getPlayerPosition())<.65&&elapsed-puddle.lastDamage>BEE_CONFIG.playerDamageCooldown){puddle.lastDamage=elapsed;this.damagePlayer(BEE_CONFIG.honeyDamage,'Honey puddle!');}if(puddle.age>5){puddle.mesh.geometry.dispose();puddle.mesh.material.dispose();this.scene.remove(puddle.mesh);this.puddles.splice(this.puddles.indexOf(puddle),1);}} }
  createPuddle(hit) { const mesh=new THREE.Mesh(new THREE.CircleGeometry(.34,14),new THREE.MeshBasicMaterial({color:0xffc928,transparent:true,opacity:.82,side:THREE.DoubleSide})); const normal=hit.face?.normal.clone().transformDirection(hit.object.matrixWorld).normalize()||new THREE.Vector3(0,1,0);mesh.position.copy(hit.point).addScaledVector(normal,.015);mesh.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1),normal);this.scene.add(mesh);this.puddles.push({mesh,age:0,lastDamage:-Infinity}); }
  removeProjectile(p) { p.mesh.geometry.dispose();p.mesh.material.dispose();this.scene.remove(p.mesh);this.projectiles.splice(this.projectiles.indexOf(p),1); }
  takeDamage(amount) { if(this.isDisposed||this.state==='DEFEATED')return false;this.health=Math.max(0,this.health-amount);this.hitFlashUntil=performance.now()+90;this.status?.(`Honey bee boss hit — ${this.health}/${BEE_CONFIG.maxHealth} HP.`);if(!this.health){this.state='DEFEATED';this.projectiles.slice().forEach(p=>this.removeProjectile(p));this.status?.('Honey bee boss defeated.');this.onDefeated?.();}return true; }
  dispose() { if(this.isDisposed)return;this.isDisposed=true;this.projectiles.slice().forEach(p=>this.removeProjectile(p));this.puddles.forEach(p=>{p.mesh.geometry.dispose();p.mesh.material.dispose();this.scene.remove(p.mesh);});this.puddles=[];this.mixer?.stopAllAction();this.mixer?.uncacheRoot(this.root);this.scene.remove(this.root);disposeObject(this.root); }
}
